#include <stdio.h>

int main(){
  
  int request;
  float balance;
  scanf("%d %f",&request, &balance);
  //check for input
  if (balance < (float)request+0.50||request%5!=0) {
  printf("%.2f\n",balance);
  return 0;  
  }
  printf("%.2f\n",(balance-(float)request-0.50));
  return 0;  
}